/**
 * \file            functions.h
 * \brief           Funciones - Archivos Header - Función definida por el usuario
 * \author          Marcos Goyret
 * \date            Jun 6, 2019
 * \details         Usar bash script para compilar, linkear y ejecutar
 */

#include <stdio.h>

/* 1. Declaración de la función "calcular_derivada_discreta". También se le conoce como "prototipo" */
void calcular_derivada_discreta(int vectorIn[], int vectorOut[], unsigned int largo);

/* 2. Declaración de la función "printar". También se le conoce como "prototipo" */
void printar(int vectorIn[], unsigned int largo);